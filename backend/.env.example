# Backend config
PORT=3000
# The timezone used by cron (recommend setting at OS level too)
TZ=America/Los_Angeles

# Optional: OpenAI key if/when you wire real AI
OPENAI_API_KEY=your_key_here

# Comma-separated device IDs allowed to subscribe (optional)
ALLOWED_DEVICE_IDS=dev-001
